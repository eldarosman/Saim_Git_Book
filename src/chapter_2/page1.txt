Make a Name for Yourself
In this puzzle, you'll add a property-value pair to an object. Then, use that property in a print() function.

INSTRUCTIONS
Add a last property inside the name object and add a string as the value
Update the print() function to print the value of name.last